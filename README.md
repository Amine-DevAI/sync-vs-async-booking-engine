# sync-vs-async-booking-engine
ooking engine built 3 ways to benchmark sync vs. async architecture — naive baseline → blocking side-effects → Redis-queued background jobs, with a custom C++ load tester and real performance data.
